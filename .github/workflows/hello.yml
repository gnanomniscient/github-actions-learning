name: First Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Linux Commands
        run: |
          pwd
          whoami
          ls -ltr
